// 아래에 코드를 작성해 주세요.

const arr = [10, 20, 30, 40, 50];
const arrayOf = ["of", "는", "배열"];

for (const item of arr) {
    document.write(item + "<br>"); // 10, 20, 30 출력
}

for (const i of arrayOf) {
    document.write(i + "<br>");
}